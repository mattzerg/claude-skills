#!/usr/bin/env bash
# Convenience wrapper — put this directory on PATH or symlink ~/.local/bin/scansnap → here.
exec /usr/bin/python3 "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")/scansnap_bridge.py" "$@"
