# Virtual environments
.venv/
venv/
__pycache__/
*.pyc

# Credentials and tokens (sensitive)
credentials.json
tokens/
accounts.json

# OS files
.DS_Store
*.swp

# IDE
.idea/
.vscode/
slack-skill/config.json
slack-skill/.bridge.pid
slack-skill/bridge.log
slack-skill/inbox.jsonl
slack-skill/pending_work.json

# Twilio SMS skill (sensitive)
twilio-sms/config.json
twilio-sms/.bridge.pid
twilio-sms/bridge.log
twilio-sms/inbox.jsonl
twilio-sms/conversations.json

# Zmail skill (sensitive)
zmail/config.json

# Alexa skill (sensitive)
alexa-skill/config.json
alexa-skill/node_modules/
alexa-skill/package-lock.json

# Google Calendar skill (sensitive)
gcal-skill/tokens/
gcal-skill/accounts.json

# Linear skill (sensitive)
linear-skill/config.json

# LinkedIn skill (sensitive)
linkedin-skill/credentials.json
linkedin-skill/tokens/
linkedin-skill/accounts.json

# Gamma skill (sensitive)
gamma-skill/config.json

# Notion skill (sensitive)
notion-skill/config.json

# CRM Bridge (sensitive)
crm-bridge/config.json
crm-bridge/.bridge.pid
crm-bridge/bridge.log
crm-bridge/backup_msg-*.txt

# ElevenLabs skill (sensitive)
eleven-labs-skill/config.json

# Blink skill (sensitive)
blink-skill/credentials.json
blink-skill/snapshots/

# Playwright skill (sensitive/state)
playwright-skill/browser_state.json
playwright-skill/sessions/
playwright-skill/screenshots/

# Suno skill (browser state)
suno-skill/chromium-profile/
suno-skill/output/

# FAL music output
fal-music-skill/output/

# Slack thread sessions
slack-skill/thread_sessions.json

# FAL video config
fal-video-skill/config.json

# DreamHost config (sensitive)
dreamhost-skill/config.json

# GoDaddy config (sensitive)
godaddy-skill/config.json

# Amazon skill lists (user data)
amazon-skill/lists/

# Suno skill (playwright browser state)
suno-skill/pw-profile/

# Suno skill sessions (cookies/auth state)
suno-skill/sessions/

# ScanSnap bridge skill (runtime files)
scansnap-bridge/bridge.log
scansnap-bridge/bridge.stdout.log
scansnap-bridge/bridge.stderr.log
scansnap-bridge/.bridge.pid
scansnap-bridge/.bridge.state.json
scansnap-bridge/debug_*.txt
scansnap-bridge/com.idanbeck.scansnap-bridge.plist

# Zergboard skill (sensitive)
zergboard-skill/config.json

# Voice-skill self-improvement loop run artifacts (PII: captured product reviews,
# email drafts, copyedit findings). Re-creatable; never commit.
fakematt-feedback/state/
fakematt-feedback/logs/
fakematt-feedback/sent-log.jsonl
fakematt-feedback/corrections.md
fakematt-copyedit/logs/
fakematt-copyedit/sent-log.jsonl
fakematt-copyedit/corrections.md
fakematt-email/logs/
fakematt-email/sent-log.jsonl
fakematt-email/corrections.md
fakematt-personal/logs/
fakematt-personal/sent-log.jsonl
fakematt-personal/corrections.md
launch-announcement/logs/
launch-announcement/sent-log.jsonl
launch-announcement/corrections.md
fakeidan/logs/
fakeidan/sent-log.jsonl
fakeidan/corrections.md

# pr-gate run logs (force-push events, gate decisions)
pr-gate/logs/
pr-gate/state/

# pr-gate review artifact (transient — written by run.py per invocation)
.pr-gate-review.md

# Generic catchall for any future voice-skill artifacts
**/state/202[6-9]*/
**/sent-log.jsonl
**/corrections.md

# Landing-page-skill insight scrapes (timestamped run artifacts)
landing-page-skill/insights/*_202[6-9]*.json
landing-page-skill/insights/*_202[6-9]*.png
fal-image-skill/config.json
fal-music-skill/config.json
nano-banana-pro/config.json

# Competitive-review-skill runtime cache (regenerated per run; self-corrects vault paths) — untracked 2026-06-28
competitive-review-skill/insights/state/
competitive-review-skill/insights/queue.log
